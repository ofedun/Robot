"""Class for handlink links on pages."""

from Lib.elements import BaseElement

class Link(BaseElement):
    """Class for handlink links on pages."""
    # def __init__(self, driver, element_tuple, timeout=10):
    #     self.driver = driver
    #     self.locator_type = element_tuple[0]
    #     self.locator = element_tuple[1]
    #     self.timeout = timeout
    #     super(BaseElement, self).__init__()

    # def click_link(self, item_name):
    #     """Click action."""
    #     self.element = self.driver.find_element_by_xpath()
    #     self.element.click(item_name)